# noda_turbo/.env.example

# --- PostgreSQL Configuration ---
PG_HOST=db # 'db' is the service name in docker-compose, accessible within the Docker network
PG_PORT=5432
PG_DATABASE=noda_turbo_db
PG_USER=noda_user
PG_PASSWORD=noda_password

# --- Redis Configuration ---
REDIS_HOST=redis # 'redis' is the service name in docker-compose
REDIS_PORT=6379