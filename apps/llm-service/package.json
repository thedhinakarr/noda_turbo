{
  "name": "llm-service",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'Nothing to build for this Python service'",
    "dev": "uvicorn app:app --reload --port 5001",
    "lint": "echo 'Linter not configured yet'"
  }
}
